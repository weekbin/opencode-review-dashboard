# R56 Discovery

R55 carry-over: last v6 cleanup. environment-setup.md (460 lines) is v5 subagent tooling setup (npm packages, gh CLI config, mock-server setup for subagent workflows). v6 lead-direct uses different tools — pre-commit hook + `.husky/pre-commit` + `scripts/verify-plugin-load.mjs` are the canonical env.
