# R58 Research

Fix: aria-hidden="true" on 2 layout-toggle SVGs + aria-label on #drawer-close button + aria-hidden on its SVG.

Single src/ file (review.html). No JS or i18n changes needed (drawer close is browser-only state, not user-facing i18n gap).
