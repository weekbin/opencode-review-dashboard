# R96 — i18n: 4 resolve reason chips + 2 button labels

4 hardcoded English resolve reason chip labels in showResolveReasonModal + Cancel/Resolve button labels. modal.cancel/action.resolve already exist.
