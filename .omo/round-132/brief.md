# R132 Brief — Persistent locked-review status

**Scope**: Surface R131's persisted lock on every dashboard launch by transporting `State.locked` in the server `Launch` payload, mirroring it in the UI type, and rendering a compact accessible lock banner at the top of the Stats pane before the empty-state branch. Extract the current vanilla CSS system into root `DESIGN.md`, reuse its semantic success palette, add bilingual round-specific copy, and cover the transport/render contract with focused tests. **Why**: R131 explicitly identified that an irreversible lock is only visible in the transient post-submit overlay, so reopened reviews can look editable. **Risk**: transport/type drift or rendering after the empty-state return could hide the status; ad-hoc styling could diverge from the established dashboard. **Acceptance**: `bun test src/r132-stats-lock-badge.test.ts` proves all 10 R132 contracts, `bash .husky/pre-commit` exits 0, and `take-screenshots.sh` captures the locked Stats view without layout or dark-mode regressions.
