# R59 Research

Fix: folder div gets role=button, tabindex=0, aria-expanded (initial + updated on toggle), keydown handler for Enter/Space.

Same pattern as R51 (commit-card-head). 1 src/ file. ~10 lines.
