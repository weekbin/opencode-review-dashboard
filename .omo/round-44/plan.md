# Phase 1 Architect Plan — Round 44

> Housekeeping profile → 1-paragraph plan (per SKILL profile gating)
> Inherited scope from `brief.md`: 8 R43 latent gap closures + 2 new SKILL.md patches (SG.R44.1, SG.R44.2)

## Plan (1-paragraph summary)

R44 is a housekeeping round closing 8 latent gaps from R43's missed v5.4 NO DEFERRAL close-out + writing 2 new skill patches that prevent recurrence. Lead-direct execution per R+ retro. R44 verifies R43's already-shipped fixes (Gate 4 script + skipLink quotes), then applies 6 new fixes: (Fix-3) introduce `matched()` helper to retire TS-strict `match![1]` pattern in old test files, sweep for similar drift patterns; (Fix-4) add `GET /mock-state` endpoint to mock-server.py returning pre-built state.json so Playwright can verify state-aware UI changes (mark-as-duplicated flow); (Fix-5) wire husky pre-commit via `bun install --frozen-lockfile` + verify `.git/hooks/pre-commit` exists (closes per memory 442 R30 retrofit incomplete); (Fix-6) write SKILL.md patches SG.R44.1 (Pre-Phase-4.5 Discovery Sweep — 7 mandatory commands lead MUST run before retro) and SG.R44.2 (Latent Gap Promotion Policy — decision tree for post-closure gap discovery: amend vs new round vs [never defer]); (Fix-7) tighten the test pattern guidelines; (Fix-8) Phase 4.9 expanded `gh issue list --label pm-manager-approved --state open` scan as part of self-check template. Phase 2.5 audit = 3 fast gates + SG.R27.1 verify-plugin-load. Phase 3a = 3 lens + SG.R44.1 hygiene lens (Code+Context lens kept minimal even for housekeeping — fixes the SKILL's gap that skipped them entirely). Phase 3c Playwright = only verify the new /mock-state endpoint loads. Phase 3.5 Doc Writer = SG.R29.8 skip (no doc changes in housekeeping).

## File changes (estimated)

| File | Fix | Change type |
|---|---|---|
| `scripts/test-review-ui/mock-server.py` | Fix-4 | ADD `GET /mock-state` endpoint |
| `.husky/pre-commit` (NEW file) | Fix-5 | husky pre-commit script (mirrors R35 commit pattern) |
| `.git/hooks/pre-commit` (auto-generated by husky) | Fix-5 | shell wrapper |
| `src/ui/_test-utils/regex-match.ts` (NEW) | Fix-3 | `matched()` helper |
| `src/ui/settings.test.ts`, `src/ui/i18n.test.ts`, `src/ui/conversation-bulk.test.ts` (and any others with `match![N]`) | Fix-3 | import + use helper |
| `.opencode/skills/team-dev-loop/SKILL.md` | Fix-6 + Fix-7 + Fix-8 | Append SG.R44.1 + SG.R44.2 + Phase 4.9 expansion |
| `package.json` (potentially) | Fix-5 | may already have husky devDep; verify only |

## Tests required

- For Fix-3 (matched helper): add 1 test file in `src/ui/_test-utils/` or modify new test file asserting `matched()` returns "" for no-match, group-1 value for match, undefined-safe handling.
- For Fix-4 (mock-server /mock-state): verify the existing e2e harness still loads `GET /review/test` scenarios; ADD a Playwright command using `eval()` to construct DOM with the state.
- For Fix-5 (husky): verify `.git/hooks/pre-commit` calls `bun run check && bun test` and they both pass on the current worktree.
- For Fix-6 (skill patches): no automated tests; verification via `grep "SG.R44"` post-edit.

## Hand-off items

**Must-do**:
- Use `matched()` helper per Fix-3, not ad-hoc nullish coalescing (consistent across files)
- Run all 7 Discovery Sweep commands from SG.R44.1 in this round to validate the rule
- Husky pre-commit must run `bun run check && bun test` (per R35 retro SG.R30.0 + memory 442)
- New /mock-state must NOT change existing /review/test behavior
- SKILL.md additions appended; do not rewrite earlier content

**Must-not-do**:
- Do not change R43's committed content (would require force-push amend — bad pattern, see Gap 4)
- Do not introduce new dependencies
- Do not skip Phase 4.5 retro even though housekeeping is small — per v5.4 NO DEFERRAL

## Risk register

- **Risk**: husky install breaks dev environment
  - Mitigation: run `bun install --frozen-lockfile` (no lockfile drift), verify with `bun test` post-install
- **Risk**: matched() helper breaks TS strict in subtle ways
  - Mitigation: helper has explicit nullable return type `string` (not `string | undefined`); caller always wraps `matched()` result, never `match[N]!`
- **Risk**: SKILL.md patches collide with future R5
  - Mitigation: write patches in numbered sections (SG.R44.1, SG.R44.2, SG.R44.3) — comments are date-stamped
- **Risk**: mock-server new endpoint drops existing test coverage
  - Mitigation: only ADD `/mock-state`; don't refactor existing `/review/test`

## Deferred items (R44 carry-over)

R43 #6 hide-whitespace perf + #7 COMMits panel visual cue → NOT in R44 (housekeeping scope only). These are user-facing UI bugs that should land in a future bugfix/feature round.

## Plan profile override

None — R44 is housekeeping per SG.R29.9 default + explicit user directive.
